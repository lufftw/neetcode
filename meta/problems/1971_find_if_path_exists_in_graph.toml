# Problem: Find if Path Exists in Graph
# https://leetcode.com/problems/find-if-path-exists-in-graph/

# ===== Problem Info =====
id = "1971"
slug = "1971_find_if_path_exists_in_graph"
title = "Find if Path Exists in Graph"
leetcode_id = 1971
url = "https://leetcode.com/problems/find-if-path-exists-in-graph/"

# ===== LeetCode Official Metadata =====
difficulty = "easy"
topics = ["depth_first_search", "breadth_first_search", "union_find", "graph"]
companies = ["amazon", "google", "meta"]

# ===== Roadmaps =====
roadmaps = ["graph_traversal_path"]

# ===== Ontology Tags (Problem Level) =====
api_kernels      = ["GraphDFS", "UnionFind"]
patterns         = ["graph_dfs_reachability", "union_find_connectivity"]
families         = ["graph_traversal", "connectivity"]
data_structures  = ["graph", "set", "union_find"]
algorithms       = ["dfs", "bfs", "union_find"]
related_problems = ["0547", "0684", "0841"]

# ===== Pattern Role =====
[pattern_role]
is_base_template = false
base_for_kernel = ""
derived_problems = []

# ===== File Locations =====
[files]
solution  = "solutions/1971_find_if_path_exists_in_graph.py"
generator = "generators/1971_find_if_path_exists_in_graph.py"
tests_dir = "tests/"

# ===== Solutions =====
[[solutions]]
key    = "default"
class  = "SolutionDFS"
method = "validPath"

api_kernels      = ["GraphDFS"]
patterns         = ["graph_dfs_reachability"]
families         = ["graph_traversal"]
data_structures  = ["graph", "set"]
algorithms       = ["dfs"]

role       = "variant"
variant    = "dfs_connectivity"
based_on   = ["0841#default"]
delta      = "Point-to-point reachability with early termination."
complexity = "O(V+E) time, O(V+E) space"
notes      = "Build adjacency list from edge list, DFS with early exit when destination found."

[[solutions]]
key    = "bfs"
class  = "SolutionBFS"
method = "validPath"

api_kernels      = ["GraphBFS"]
patterns         = ["graph_bfs_reachability"]
families         = ["graph_traversal"]
data_structures  = ["graph", "set", "queue"]
algorithms       = ["bfs"]

role       = "variant"
variant    = "bfs_connectivity"
based_on   = ["1971#default"]
delta      = "BFS instead of DFS."
complexity = "O(V+E) time, O(V+E) space"
notes      = "Iterative BFS with early termination."

[[solutions]]
key    = "union_find"
class  = "SolutionUnionFind"
method = "validPath"

api_kernels      = ["UnionFind"]
patterns         = ["union_find_connectivity"]
families         = ["connectivity"]
data_structures  = ["union_find"]
algorithms       = ["union_find"]

role       = "variant"
variant    = "union_find"
based_on   = []
delta      = "Union-Find approach. Good for multiple connectivity queries."
complexity = "O(E*α(n)) time, O(V) space"
notes      = "α(n) is inverse Ackermann, effectively constant. Better for multiple queries on same graph."

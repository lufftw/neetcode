# Problem: Find All Anagrams in a String
# https://leetcode.com/problems/find-all-anagrams-in-a-string/

# ===== Problem Info =====
id = "0438"
slug = "0438_find_all_anagrams_in_a_string"
title = "Find All Anagrams in a String"
leetcode_id = 438
url = "https://leetcode.com/problems/find-all-anagrams-in-a-string/"

# ===== LeetCode Official Metadata =====
difficulty = "medium"
topics = ["string", "hash_table", "sliding_window"]
companies = ["google", "amazon", "meta", "microsoft", "apple", "bloomberg"]

# ===== Roadmaps =====
roadmaps = ["neetcode_150", "sliding_window_path"]

# ===== Ontology Tags (Problem Level) =====
api_kernels      = ["SubstringSlidingWindow"]
patterns         = ["sliding_window_freq_cover"]
families         = ["substring_window"]
data_structures  = ["string", "hash_map"]
algorithms       = ["sliding_window"]
related_problems = ["0003", "0076", "0242", "0567"]

# ===== Pattern Role =====
[pattern_role]
is_base_template = false
derives_from = "0567"
delta_summary = "Fixed-size window exact frequency match, collect ALL positions"

# ===== Relationship to Base =====
[kernel_delta]
base_problem = "0567"
changes = [
    "Output: boolean → list of indices",
    "On match: return True → append to result list",
    "After match: stop → continue searching",
]

# ===== File Locations =====
[files]
solution  = "solutions/0438_find_all_anagrams_in_a_string.py"
generator = "generators/0438_find_all_anagrams_in_a_string.py"
tests_dir = "tests/0438_find_all_anagrams_in_a_string/"

# ===== Solutions =====
[[solutions]]
key    = "default"
class  = "Solution"
method = "findAnagrams"

api_kernels      = ["SubstringSlidingWindow"]
patterns         = ["sliding_window_freq_cover"]
families         = ["substring_window"]
data_structures  = ["string", "hash_map"]
algorithms       = ["sliding_window"]
related_problems = ["0567", "0076"]

role       = "variant"
variant    = "fixed_window_collect_all"
based_on   = ["0567#default"]
delta      = "Instead of returning on first match, collect all matching positions."
complexity = "O(|s| + |p|) time, O(1) space for maps, O(k) for output"
notes      = "Common pattern transformation: 'does X exist?' → 'find all X'. Fixed window size = len(pattern)."


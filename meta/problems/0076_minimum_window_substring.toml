# Problem: Minimum Window Substring
# https://leetcode.com/problems/minimum-window-substring/

# ===== Problem Info =====
id = "0076"
slug = "0076_minimum_window_substring"
title = "Minimum Window Substring"
leetcode_id = 76
url = "https://leetcode.com/problems/minimum-window-substring/"

# ===== LeetCode Official Metadata =====
difficulty = "hard"
topics = ["string", "hash_table", "sliding_window"]
companies = ["google", "meta", "amazon", "microsoft", "apple", "bloomberg", "uber", "linkedin"]

# ===== Roadmaps =====
roadmaps = ["neetcode_150", "blind_75", "grind_75", "sliding_window_path"]

# ===== Ontology Tags (Problem Level) =====
api_kernels      = ["SubstringSlidingWindow"]
patterns         = ["sliding_window_freq_cover"]
families         = ["substring_window"]
data_structures  = ["string", "hash_map"]
algorithms       = ["sliding_window", "two_pointers"]
related_problems = ["0003", "0030", "0209", "0438", "0567"]

# ===== Pattern Role =====
[pattern_role]
is_base_template = false
derives_from = "0003"
delta_summary = "Variable window minimization with frequency coverage requirement"

# ===== Relationship to Base =====
[kernel_delta]
base_problem = "0003"
changes = [
    "State: last_seen_index → need_frequency + have_frequency maps",
    "Goal: maximize → minimize (contract WHILE valid, not UNTIL valid)",
    "Invariant: unique chars → covers all required chars with frequencies",
    "Optimization: satisfied counter for O(1) validity check",
]

# ===== File Locations =====
[files]
solution  = "solutions/0076_minimum_window_substring.py"
generator = "generators/0076_minimum_window_substring.py"
tests_dir = "tests/0076_minimum_window_substring/"

# ===== Solutions =====
[[solutions]]
key    = "default"
class  = "Solution"
method = "minWindow"

api_kernels      = ["SubstringSlidingWindow"]
patterns         = ["sliding_window_freq_cover"]
families         = ["substring_window"]
data_structures  = ["string", "hash_map"]
algorithms       = ["sliding_window", "two_pointers"]
related_problems = ["0003", "0567", "0438"]

role       = "variant"
variant    = "minimize_freq_cover"
based_on   = ["0003#default"]
delta      = "Track need/have frequencies; minimize window while covering all required chars."
complexity = "O(|s| + |t|) time, O(|t|) space"
notes      = "Demonstrates the MINIMIZE pattern: contract WHILE valid (opposite of maximize pattern in LC3)."


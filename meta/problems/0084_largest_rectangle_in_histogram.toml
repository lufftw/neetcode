# Problem: Largest Rectangle in Histogram
# https://leetcode.com/problems/largest-rectangle-in-histogram/

# ===== Problem Info =====
id = "0084"
slug = "0084_largest_rectangle_in_histogram"
title = "Largest Rectangle in Histogram"
leetcode_id = 84
url = "https://leetcode.com/problems/largest-rectangle-in-histogram/"

# ===== LeetCode Official Metadata =====
difficulty = "hard"
topics = ["array", "stack", "monotonic_stack"]
companies = ["google", "amazon", "meta", "microsoft", "apple", "bloomberg"]

# ===== Roadmaps =====
roadmaps = ["neetcode_150", "blind_75", "monotonic_stack_path"]

# ===== Ontology Tags (Problem Level) =====
api_kernels      = ["MonotonicStack"]
patterns         = ["histogram_max_rectangle"]
families         = ["stack_monotonic"]
data_structures  = ["array", "stack"]
algorithms       = ["monotonic_stack"]
related_problems = ["0085", "0042"]

# ===== Pattern Role =====
[pattern_role]
is_base_template = true
base_for_kernel = "MonotonicStack"
derived_problems = ["0085"]

# ===== File Locations =====
[files]
solution  = "solutions/0084_largest_rectangle_in_histogram.py"
generator = "generators/0084_largest_rectangle_in_histogram.py"
tests_dir = "tests/"

[[solutions]]
key    = "default"
class  = "SolutionSentinel"
method = "largestRectangleArea"

api_kernels      = ["MonotonicStack"]
patterns         = ["histogram_max_rectangle"]
families         = ["stack_monotonic"]
data_structures  = ["array", "stack"]
algorithms       = ["monotonic_stack"]

role       = "base"
variant    = ""
based_on   = []
delta      = ""
complexity = "O(n) time, O(n) space"
notes      = "Single-pass with sentinel for cleaner termination."

[[solutions]]
key    = "twopass"
class  = "SolutionTwoPass"
method = "largestRectangleArea"

api_kernels      = ["MonotonicStack"]
patterns         = ["histogram_max_rectangle"]

role       = "variant"
variant    = "two_pass"
based_on   = ["0084#default"]
delta      = "Precompute left and right boundaries separately."
complexity = "O(n) time, O(n) space"

# Problem: Reverse Linked List
# https://leetcode.com/problems/reverse-linked-list/

# ===== Problem Info =====
id = "0206"
slug = "0206_reverse_linked_list"
title = "Reverse Linked List"
leetcode_id = 206
url = "https://leetcode.com/problems/reverse-linked-list/"

# ===== LeetCode Official Metadata =====
difficulty = "easy"
topics = ["linked_list", "recursion"]
companies = ["google", "amazon", "meta", "microsoft", "apple", "bloomberg"]

# ===== Roadmaps =====
roadmaps = ["neetcode_150", "blind_75", "grind_75", "leetcode_top_100", "linked_list_in_place_reversal_path"]

# ===== Ontology Tags (Problem Level) =====
api_kernels      = ["LinkedListInPlaceReversal"]
patterns         = ["reverse_entire_list"]
families         = ["linked_list_manipulation"]
data_structures  = ["linked_list"]
algorithms       = ["two_pointers", "recursion"]
related_problems = ["0092", "0025", "0234"]

# ===== File Locations =====
[files]
solution  = "solutions/0206_reverse_linked_list.py"
generator = "generators/0206_reverse_linked_list.py"
tests_dir = "tests/0206_reverse_linked_list/"

# ===== Pattern Role =====
[pattern_role]
is_base_template = true
base_for_kernel  = "LinkedListInPlaceReversal"
derived_problems = ["0092", "0025"]

# ===== Solutions =====
[[solutions]]
key    = "default"
class  = "SolutionIterative"
method = "reverseList"

api_kernels      = ["LinkedListInPlaceReversal"]
patterns         = ["reverse_entire_list"]
families         = ["linked_list_manipulation"]
data_structures  = ["linked_list"]
algorithms       = ["two_pointers"]
related_problems = ["0092", "0025"]

role       = "base"
variant    = ""
based_on   = []
delta      = ""
complexity = "O(N) time, O(1) space"
notes      = "Iterative three-pointer reversal. Canonical base template for in-place linked list reversal."

[[solutions]]
key    = "iterative"
class  = "SolutionIterative"
method = "reverseList"

api_kernels      = ["LinkedListInPlaceReversal"]
patterns         = ["reverse_entire_list"]
families         = ["linked_list_manipulation"]
data_structures  = ["linked_list"]
algorithms       = ["two_pointers"]
related_problems = ["0092", "0025"]

role       = "base"
variant    = ""
based_on   = []
delta      = ""
complexity = "O(N) time, O(1) space"
notes      = "Same as default. Iterative three-pointer reversal."

[[solutions]]
key    = "recursive"
class  = "SolutionRecursive"
method = "reverseList"

api_kernels      = ["LinkedListInPlaceReversal"]
patterns         = ["reverse_entire_list"]
families         = ["linked_list_manipulation"]
data_structures  = ["linked_list"]
algorithms       = ["recursion"]
related_problems = ["0092", "0025"]

role       = "variant"
variant    = "recursive"
based_on   = ["0206#default"]
delta      = "Recursive approach instead of iterative. O(N) stack space."
complexity = "O(N) time, O(N) space"
notes      = "Recursive reversal - reverse tail first, fix on return."
